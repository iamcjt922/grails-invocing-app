package com.example

class Invoice {
    Customer customer
    BigDecimal amount
    Date dateCreated = new Date()

    static constraints = {
        customer nullable: false
        amount min: 0.0
    }
}
